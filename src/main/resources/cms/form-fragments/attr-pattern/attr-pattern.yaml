kind: "FormFragment"
title: "Attr: Pattern"
form:
  - type: "TextLine"
    name: "pattern"
    label: "Pattern (regex)"
    occurrences:
      min: 0
      max: 1
    helpText: "[Optional] For advanced users/developers: Set a regex expression to validate input, e.g. phone numbers, email addresses, etc. Do not use this if you do are not experienced with regexes, as an incorrect regex can prevent users from completing your form."
